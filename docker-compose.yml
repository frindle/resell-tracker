services:
  app:
    build: .
    network_mode: host
    volumes:
      - /mnt/user/appdata/reselling:/data
    environment:
      DATABASE_URL: file:/data/resell.db
      PORT: 3003
    restart: unless-stopped
