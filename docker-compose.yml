services:
  app:
    build: .
    ports:
      - "3003:3000"
    volumes:
      - /mnt/user/appdata/reselling:/data
    environment:
      DATABASE_URL: file:/app/data/resell.db
    restart: unless-stopped
