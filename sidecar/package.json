{
  "name": "resell-tracker-sidecar",
  "version": "1.0.0",
  "private": true,
  "description": "Headless Amazon/Walmart order sync — same ExtensionCommand queue as the browser extension, driven by a real headed Chrome under Xvfb instead of the user's own browser.",
  "type": "commonjs",
  "scripts": {
    "poll": "node src/poll.js",
    "login": "node src/login.js"
  },
  "dependencies": {
    "playwright-core": "^1.48.0"
  }
}
